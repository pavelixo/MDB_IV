# Banco de Dados — EduLearn

Projeto completo de modelagem e manipulação de dados para o sistema fictício **EduLearn**, desenvolvido como atividade prática de banco de dados.

## 📌 Conteúdo do Repositório

- **Modelo lógico e DER**
- **Scripts SQL completos**
  - Criação das tabelas (`create.sql`)
  - Inserção de dados (`insert.sql`)
  - Consultas (`select.sql`)
  - Atualizações (`update.sql`)
  - Deleções (`delete.sql`)
- Documentação técnica em `/docs`

## 🛠 Tecnologias Utilizadas
- MySQL Workbench (ou PostgreSQL/pgAdmin)
- SQL (DDL e DML)

## ▶ Como Executar

1. Abra o Workbench/pgAdmin.
2. Execute `create.sql`.
3. Execute `insert.sql`.
4. Teste as consultas em `select.sql`.
5. Use `update.sql` e `delete.sql` para manipulações adicionais.

